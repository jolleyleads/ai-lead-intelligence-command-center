services:
  - type: web
    name: ai-lead-intelligence-command-center
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m flask --app app run --host 0.0.0.0 --port $PORT
    autoDeploy: true
