services:
  - type: web
    name: ai-lead-intelligence-command-center
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: BOOKING_LINK
        value: https://calendly.com/your-link/demo
